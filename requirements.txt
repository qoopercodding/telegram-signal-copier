# Telegram
telethon>=1.36
cryptg>=0.4              # Faster encryption for Telethon media

# Configuration & Validation
pydantic>=2.11
pydantic-settings>=2.9
python-dotenv>=1.1

# AI / LLM
google-genai>=1.0         # Google Gemini API (vision + text, nowe SDK)
anthropic>=0.40          # Claude Haiku fallback
openai>=1.50              # GPT-4o-mini fallback
yfinance>=0.2              # Walidacja tickerów (GPW + globalne)

# HTTP
httpx>=0.28

# Logging
loguru>=0.7

# Testing
pytest>=8.0
pytest-asyncio>=0.25
